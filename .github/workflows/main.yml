name: Ping Render Server

on:
  schedule:
    # кожні 14 хв з 08:00 до 23:00 за Києвом (UTC+3 => 05-20)
    - cron: "*/10 5-20 * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Send request to Render
        run: curl -s https://team-online-storage.onrender.com/api/v1/livez/ > /dev/null
